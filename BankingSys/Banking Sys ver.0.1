#include <iostream>

using namespace std;

typedef struct
{
	int accID;
	char cusName[20];
	int balance;
}BankAccount;

BankAccount acc[2] = { 0 };

void showMenu(void);
void MakeAccount(void);
void DepositMoney(void);
void WithdrawMoney(void);
void ShowAllAccInfo(void);

int main(void)
{
	int choice;

	while (1)
	{
		showMenu();
		cout << "선택: ";
		cin >> choice;
		cout << endl;
		switch (choice)
		{
			switch (choice)
			{
			case 1:
				MakeAccount();
				break;
			case 2:
				DepositMoney();
				break;
			case 3:
				WithdrawMoney();
				break;
			case 4:
				ShowAllAccInfo();
				break;
			case 5:
				exit(1);
			defalult:
				cout << "잘못된 입력입니다 다시 입력하세요" << endl;
			}
		}
	}
	return 0;
}
void showMenu(void)
{
	cout << "=======menu=======" << endl;
	cout << "1. 계좌 개설" << endl;
	cout << "2. 입금" << endl;
	cout << "3. 출금" << endl;
	cout << "4. 잔액 조회" << endl;
	cout << "5. 프로그램 종료" << endl;
	cout << "=======menu=======" << endl;	
}
void MakeAccount(void)
{
	int i = 0;
	cout << "===============MakeAccount===============" << endl;
	cout << "customer's Account number: ";
	cin >> acc[i].accID;
	cout << "customer's Name: ";
	cin >> acc[i].cusName;
	cout << "customer's Account balance: ";
	cin >> acc[i].balance;

	cout << "=========complete Make Account ===========" << endl;
}
void DepositMoney(void) // 없는 계좌를 넣으면 없다고 안내
{
	int i = 0;
	int deposit = 0;
	cout << "계좌 번호: ";
	cin >> acc[i].accID;
	cout << "입금액: ";
	cin >> acc[i].balance;
	deposit += acc[i].balance;
}
void WithdrawMoney(void) // 소지금보다 많이 빠지면 잔액부족 안내
{
	int i = 0;
	int withdraw = 0;
	cout << "계좌 번호: ";
	cin >> acc[i].accID;
	cout << "출금액: ";
	cin >> acc[i].balance;
	withdraw -= acc[i].balance;
}
void ShowAllAccInfo(void)
{
	int i = 0;
	for (i = 0; i < 2;i++)
	{
		cout << "고객명: " << acc[i].cusName << endl;
		cout << "계좌번호: " << acc[i].accID << endl;
		cout << "계좌잔액: " << acc[i].balance << endl;
	}
	
}
